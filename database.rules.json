{
  "rules": {
    ".read": false,
    ".write": false,
    
    "presence": {
      "$userID": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid == $userID"
      }
    }
  }
}

